# PURPOSE: Documents required frontend environment variables without storing real secrets.
# USAGE: Copy to `.env` locally and update values for the current backend environment.

VITE_API_URL=http://localhost:8000/api
VITE_APP_NAME=GodownAdmin
